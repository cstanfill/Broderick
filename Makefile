clean::
	rm -f *~
	rm -f \#*
	rm -f *.pyc
	rm -f logs/*~
	rm -f logs/\#*
	rm -f logs/*.pyc
	rm -f modules/*~
	rm -f modules/\#*
	rm -f modules/*.pyc
	rm -f modules_squeeze/*~
	rm -f modules_squeeze/\#*
	rm -f modules_squeeze/*.pyc
	clear
	ls -A
###

